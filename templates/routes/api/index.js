/**
* /routes/api/index.js
*
* @description:: Index file for the api mini-application. All routes with "/api"
* come through here.
*
*/

const APIRouter = require("express").Router();
const policies = require("./policies");

// Put route handlers here

module.exports = APIRouter;
