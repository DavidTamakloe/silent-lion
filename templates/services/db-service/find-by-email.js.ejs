const <%= modelName %> = require("../../../models/<%= entityName %>.js");

module.exports = function(email, relationString="") {
    return <%= modelName %>.query().where({
        email: email
    }).eager(relationString).first();
};
