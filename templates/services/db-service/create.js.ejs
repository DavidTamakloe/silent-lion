const <%= modelName %>= require("../../../models/<%= entityName %>.js");

module.exports = function(args) {
    return <%= modelName %>.query().insert(
        {
            //put create fields Here
            //field: args.field
            <%if (typeof fields !== "undefined"){ %>
            <% fields.forEach(function(field){ %>
            <%= field %> : args.<%= field %>,
            <% }) %>
            <% } %>
        }
    );
};
